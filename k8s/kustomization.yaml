apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: llm-stack

resources:
  - namespace.yaml
  - postgres.yaml
  - phoenix.yaml
  - litellm.yaml

secretGenerator:
  - name: llm-stack-secrets
    envs:
      - .env
    type: Opaque
